<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Albums - Home</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Music Albums</h1>
  <ul>
    <% albums.forEach(album => { %>
      <li>
        <strong><%= album.title %></strong> by <%= album.artist %> 
        (<%= album.year %>)
        <br>
        <a href="/details/<%= album.title %>">View Details</a>
      </li>
    <% }) %>
  </ul>
</body>
</html>
